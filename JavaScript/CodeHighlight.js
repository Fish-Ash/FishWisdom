// document.addEventListener("DOMContentLoaded", function() {
//     const codeBlocks = document.querySelectorAll('code.cpp');
    
//     codeBlocks.forEach(block => {
//         let html = block.innerHTML;
//         html = html.replace(/\/\/.*/g, match => `<span class="comment">${match}</span>`);
//         block.innerHTML = html;
//     });
// });

// document.addEventListener("DOMContentLoaded", function() {
//     const textElements = document.querySelectorAll('pre.code');
//     textElements.forEach(textElement => {
//         textElement.textContent = textElement.textContent.replace(/ /g, '');
//     });
// });